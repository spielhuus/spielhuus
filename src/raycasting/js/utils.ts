import { Vector2 } from '../../js/vector';

export interface DrawOptions {
  gridSize: Vector2;
  lineColor: string;
  lineWidth: number;
  line_radius: number;
  circle_radius: number;
  circle_color: string;
  wallColor: string;
}
